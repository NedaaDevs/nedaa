export enum AppLocale {
  EN = "en",
  AR = "ar",
  UR = "ur",
  MS = "ms",
}

export enum PlatformType {
  IOS = "ios",
  ANDROID = "android",
}

export enum AppMode {
  SYSTEM = "system",
  LIGHT = "light",
  DARK = "dark",
}

export enum AppDirection {
  RTL = "rtl",
  LTR = "ltr",
}
