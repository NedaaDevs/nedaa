export enum AppLocale {
  EN = "en",
  AR = "ar",
  // MS = "ms",
  UR = "ur",
}

export enum PlatformType {
  IOS = "ios",
  ANDROID = "android",
}

export enum AppMode {
  SYSTEM = "system",
  LIGHT = "light",
  DARK = "dark",
}

export enum AppDirection {
  RTL = "rtl",
  LTR = "ltr",
}
