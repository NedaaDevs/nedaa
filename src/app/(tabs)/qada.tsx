import { Background } from "@/components/ui/background";
import QadaScreen from "@/components/Qada/QadaScreen";

const Qada = () => {
  return (
    <Background>
      <QadaScreen />
    </Background>
  );
};

export default Qada;
