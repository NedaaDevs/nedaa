export enum AppLocale {
  EN = "en",
  AR = "ar",
  MS = "ms",
}

export enum PlatformType {
  IOS = "ios",
  ANDROID = "android",
}

export enum AppMode {
  LIGHT = "light",
  DARK = "dark",
}

export enum AppDirection {
  RTL = "rtl",
  LTR = "ltr",
}
